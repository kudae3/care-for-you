import React from "react";

const page = () => {
  return (
    <div>
      <p className="text-accent">
        page <button className="primary-btn">Hello click me</button>{" "}
      </p>
    </div>
  );
};

export default page;
